# Практическое задание 2: Структурированный вывод
## Время: 25 минут

---

## 🎯 Цель практики

Студенты научатся создавать промпты с структурированным выводом в форматах JSON, XML, Markdown, CSV для последующей автоматизации и обработки данных.

**Результат:** Минимум 3 промпта с разными форматами вывода, протестированных в моделях.

---

## 📋 Инструкция для преподавателя

### Тайминг:
- **0-2 мин:** Объяснение задания и демонстрация одного примера
- **2-17 мин:** Студенты работают самостоятельно
- **17-22 мин:** Разбор – 2-3 студента показывают результаты
- **22-25 мин:** Обсуждение и выводы

### Что говорить в начале:

**"Вторая практика – структурированный вывод.**

**Почему это важно:**
Когда модель выдает результат в формате JSON, XML или CSV – его можно обработать кодом. Это открывает двери к автоматизации.

**Пример:**
Вы анализируете 100 отзывов. Вместо текста получаете JSON – загружаете в базу данных или Excel.

**Задание:**
Создайте минимум 3 промпта с структурированным выводом:
1. Один с JSON
2. Один с Markdown-таблицей
3. Один с любым другим форматом (XML, CSV, YAML)

**Правило:** Всегда показывайте модели пример структуры – она скопирует формат.

**На работу – 15 минут. Поехали!"**

---

## 📝 Сценарии для студентов

---

### Сценарий 1: Анализ отзывов → JSON

**Задача:**
Проанализируйте отзыв о продукте и выдайте результат в JSON-формате.

**Пример отзыва:**
```
"Заказал смартфон для работы. Пришел за 2 дня – очень быстро!
Экран отличный, батарея держит долго. Единственное – камера
могла бы быть лучше, особенно в темноте. В целом доволен,
рекомендую за такую цену."
```

**Структура JSON (покажите модели):**
```json
{
  "sentiment": "positive/neutral/negative",
  "rating": 1-5,
  "pros": ["список плюсов"],
  "cons": ["список минусов"],
  "topics": ["список тем"],
  "recommendation": true/false
}
```

**Ваша задача:**
Создайте промпт, который проанализирует отзыв и выдаст JSON по этой структуре.

**Подсказка:**
```
Ты – аналитик отзывов.

Проанализируй отзыв: "[вставьте отзыв]"

Выдай результат строго в JSON:
{
  "sentiment": "...",
  ...
}

Только JSON, без дополнительного текста.
```

---

### Сценарий 2: Генерация FAQ → Markdown-таблица

**Задача:**
Создайте FAQ (10 вопросов и ответов) для онлайн-курса по Python для новичков.

**Формат вывода:**
Markdown-таблица:
```markdown
| Вопрос | Ответ | Категория |
|--------|-------|-----------|
| ...    | ...   | ...       |
```

**Категории:** "Технические", "Оплата", "Процесс обучения", "Сертификат"

**Ваша задача:**
Создайте промпт, который сгенерирует таблицу FAQ в Markdown.

**Подсказка:**
- Укажите роль (специалист по продукту? методист?)
- Опишите задачу
- Покажите пример таблицы
- Укажите: "Строго в Markdown, без дополнительного текста"

---

### Сценарий 3: Извлечение данных из текста → JSON

**Задача:**
Извлеките ключевую информацию из описания вакансии.

**Пример описания:**
```
"Ищем Frontend-разработчика (React, TypeScript) в нашу команду.
Опыт от 2 лет. Зарплата: 2500-3500$ в зависимости от опыта.
Удаленная работа, полная занятость. Офис в Минске, но можно
работать откуда угодно. Знание английского B1+."
```

**Структура JSON:**
```json
{
  "position": "название позиции",
  "required_skills": ["список скиллов"],
  "experience_years": число,
  "salary_range": {
    "min": число,
    "max": число,
    "currency": "USD/EUR/..."
  },
  "work_format": "remote/office/hybrid",
  "location": "город",
  "english_level": "A1/A2/B1/B2/C1/C2"
}
```

**Ваша задача:**
Создайте промпт, который извлечет данные и выдаст JSON.

---

### Сценарий 4: Создание расписания → CSV

**Задача:**
Создайте расписание занятий для онлайн-курса (12 занятий, 3 занятия в неделю).

**Формат вывода:** CSV

**Структура:**
```csv
Номер,Тема,Дата,Длительность,Тип
1,Введение в Python,2026-03-02,2 часа,Лекция
...
```

**Параметры:**
- Курс: "Основы Python для аналитиков"
- Старт: 2 марта 2026
- График: понедельник, среда, пятница, 19:00
- Типы занятий: Лекция, Практика, Проект

**Ваша задача:**
Создайте промпт, который сгенерирует CSV-файл расписания.

---

### Сценарий 5: Конвертация данных → JSON

**Задача:**
Преобразуйте неструктурированный текст в структурированный JSON.

**Входной текст:**
```
"Встреча с клиентом состоится в среду, 15 марта, в 14:00.
Участники: Иван Петров (наша сторона), Анна Смирнова (клиент).
Тема: Обсуждение нового проекта. Место: Zoom. Нужно подготовить
презентацию и смету."
```

**Структура JSON:**
```json
{
  "event_type": "meeting/call/...",
  "date": "YYYY-MM-DD",
  "time": "HH:MM",
  "participants": [
    {
      "name": "Имя",
      "role": "client/team"
    }
  ],
  "topic": "тема встречи",
  "location": "место или платформа",
  "preparations": ["список подготовки"]
}
```

**Ваша задача:**
Создайте промпт для конвертации.

---

### Сценарий 6: Генерация тестовых данных → JSON

**Задача:**
Сгенерируйте 5 фейковых профилей пользователей для тестирования приложения.

**Структура JSON:**
```json
{
  "users": [
    {
      "id": число,
      "name": "Имя Фамилия",
      "email": "email@example.com",
      "age": число,
      "occupation": "профессия",
      "interests": ["список интересов"]
    }
  ]
}
```

**Требования:**
- Реалистичные данные (но вымышленные)
- Разные возрастные группы (25-55 лет)
- Разные профессии

**Ваша задача:**
Создайте промпт для генерации тестовых данных.

---

### Сценарий 7: Сравнительная таблица → Markdown

**Задача:**
Создайте сравнительную таблицу 3 CRM-систем для малого бизнеса.

**Критерии сравнения:**
- Цена (в месяц)
- Функции (базовые возможности)
- Интеграции (с какими сервисами работает)
- Сложность (легко / средне / сложно)
- Поддержка (есть / нет)

**Формат:** Markdown-таблица

**CRM-системы:** HubSpot, Bitrix24, amoCRM (или любые другие)

**Ваша задача:**
Создайте промпт для генерации таблицы сравнения.

---

### Сценарий 8: Категоризация задач → JSON

**Задача:**
Проанализируйте список задач и распределите их по категориям.

**Список задач:**
```
1. Исправить баг с авторизацией
2. Написать статью в блог про новую функцию
3. Провести интервью с кандидатом на позицию дизайнера
4. Обновить документацию API
5. Оптимизировать скорость загрузки главной страницы
6. Созвониться с клиентом по проекту
```

**Структура JSON:**
```json
{
  "tasks": [
    {
      "id": число,
      "title": "название задачи",
      "category": "Development/Marketing/HR/Sales",
      "priority": "High/Medium/Low",
      "estimated_time": "время в часах"
    }
  ]
}
```

**Ваша задача:**
Создайте промпт для категоризации.

---

## 🔥 Продвинутые сценарии (для тех, кто быстро)

### Сценарий 9: Многоуровневый JSON
Создайте промпт, который генерирует JSON с вложенными объектами (например, структура меню сайта с подразделами).

### Сценарий 10: XML-конфигурация
Создайте промпт, который генерирует XML-конфигурацию для веб-сервера (пример: настройки виртуального хоста).

### Сценарий 11: YAML для CI/CD
Создайте промпт, который генерирует YAML-файл для GitHub Actions (пример: автоматический запуск тестов при push).

---

## 💡 Советы для студентов

### Как добиться структурированного вывода:

1. **Покажите модели пример структуры**
   ```
   Выдай результат в JSON:
   {
     "поле1": "значение",
     "поле2": ["массив"]
   }
   ```

2. **Укажите: "Только JSON, без текста"**
   Иначе модель может добавить объяснения до или после JSON.

3. **Проверьте валидность**
   Скопируйте результат в JSON-валидатор (например, jsonlint.com)

4. **Если модель добавляет лишний текст:**
   Добавьте в промпт: "Ответь ТОЛЬКО структурой, без вступлений и пояснений"

---

## 🔍 Что смотреть, ходя между студентами

### Частые ошибки:

1. **Модель выдает JSON, но с лишним текстом**
   → Подскажите: "Добавь в промпт: 'Только JSON, без текста'"

2. **JSON невалидный (не хватает кавычек, запятых)**
   → "Проверь в JSON-валидаторе. Если ошибка – уточни промпт: 'Валидный JSON'"

3. **Студент не показал модели пример структуры**
   → "Покажи модели пример – она скопирует формат точнее"

4. **Markdown-таблица плохо отформатирована**
   → "Укажи в промпте: 'Правильный Markdown-синтаксис таблицы'"

---

## 📢 Разбор результатов (17-22 мин)

### Формат:

**Что говорить:**
"Время! Кто хочет показать свой JSON/таблицу/CSV? Нужны 2-3 добровольца."

**Что обсудить:**
1. Студент показывает промпт
2. Показывает результат модели
3. **Проверка валидности:** скопируйте JSON и проверьте в валидаторе (покажите на экране)
4. Обсуждение: что можно улучшить?

**Вопросы для обсуждения:**
- Модель выдала чистый формат или добавила текст?
- JSON валидный?
- Таблица отформатирована правильно?
- Что можно добавить в промпт, чтобы результат был еще точнее?

---

## 🎯 Демонстрация: Проверка валидности JSON

**Покажите студентам в реальном времени:**

1. Откройте https://jsonlint.com
2. Скопируйте JSON из результата модели
3. Вставьте в валидатор
4. Если ошибки – покажите, как их исправить
5. Если валидный – покажите: "Зеленый свет – можно использовать"

**Что говорить:**
"Всегда проверяйте JSON в валидаторе. Если модель ошиблась – уточните промпт."

---

## ⚠️ Что делать, если...

### ...модель добавляет текст до/после JSON?
→ Уточните промпт:
```
Ответь ТОЛЬКО JSON-структурой, без вступлений, объяснений или заключений.
```

### ...JSON невалидный (ошибки синтаксиса)?
→ Добавьте в промпт:
```
Выдай валидный JSON, который можно парсить без ошибок.
```

### ...Markdown-таблица "поехала"?
→ Добавьте:
```
Используй правильный Markdown-синтаксис таблиц с разделителями |---|
```

### ...модель выдает не тот формат (просили JSON, а она выдала текст)?
→ Будьте категоричнее:
```
ВАЖНО: Ответь СТРОГО в формате JSON. Любой другой формат неприемлем.
```

---

## ✅ Критерии успешного выполнения

Студент справился, если:
- [ ] Создал минимум 3 промпта с разными форматами
- [ ] JSON валидный (проверено в валидаторе)
- [ ] Markdown-таблица отформатирована правильно
- [ ] Модель выдает чистый формат без лишнего текста
- [ ] Сохранил промпты в свою библиотеку

---

## 📊 Шаблон для студентов

```markdown
# Структурированный вывод – Практика 2

## Промпт 1: JSON (Сценарий: _________________)

**Промпт:**
[вставьте ваш промпт]

**Результат:**
```json
[вставьте JSON]
```

**Валидность:** ✅ валидный / ❌ невалидный

**Что улучшить:**


---

## Промпт 2: Markdown (Сценарий: _________________)

**Промпт:**

**Результат:**
[вставьте таблицу]

**Что улучшить:**


---

## Промпт 3: Другой формат (Сценарий: _________________)

**Формат:** CSV / XML / YAML

**Промпт:**

**Результат:**

**Что улучшить:**


---

## 💡 Выводы

Какой формат было сложнее всего получить?


Какой прием помог добиться чистого вывода?

```

---

## 🎯 Итоги практики (что говорить в конце)

**"Отлично! Теперь вы умеете получать структурированные данные из моделей.**

**Что мы поняли:**
1. Модели могут выдавать результат в любом формате – JSON, CSV, XML, Markdown
2. Ключ к успеху: показать модели пример структуры
3. Валидация важна – всегда проверяйте JSON

**Где применять:**
- Парсинг больших объемов данных (отзывы, резюме, документы)
- Автоматизация: JSON → скрипт обработки → база данных
- Интеграции: модель → структура → API

**Это мощный инструмент для автоматизации рутины.**

Сохраните эти промпты! Они пригодятся.

Вопросы? Отлично. Перерыв 10 минут, затем третья практика."

---

## 📁 Эталонные решения

См. файл `07-ОТВЕТЫ-И-РЕШЕНИЯ.md` (раздел "Практика 2")

---

## 🔗 Полезные инструменты для проверки форматов

**Раздайте студентам список:**

- **JSON:** https://jsonlint.com
- **XML:** https://www.xmlvalidation.com
- **YAML:** https://www.yamllint.com
- **Markdown tables:** https://www.tablesgenerator.com/markdown_tables
- **CSV preview:** https://www.convertcsv.com/csv-viewer-editor.htm

---

**Удачи! 🚀**
