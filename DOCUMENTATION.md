# Документация платформы Prompt Engineering 2.0

## Содержание

1. [Обзор платформы](#обзор-платформы)
2. [Структура проекта](#структура-проекта)
3. [Главная страница курса](#главная-страница-курса)
4. [Модуль 3: Интерактивная платформа](#модуль-3-интерактивная-платформа)
5. [Руководство для преподавателя](#руководство-для-преподавателя)
6. [Руководство для студента](#руководство-для-студента)
7. [Добавление новых модулей](#добавление-новых-модулей)
8. [Техническая документация](#техническая-документация)
9. [Кастомизация](#кастомизация)

---

## Обзор платформы

**Prompt Engineering 2.0** — интерактивная обучающая платформа для курса по промпт-инжинирингу. Платформа заменяет традиционные презентации на интерактивные веб-приложения с:

- Прогресс-трекингом
- Квизами и проверкой знаний
- Режимами студента/преподавателя
- Интерактивными демо
- Системой блокировки уроков

**Технологии:**
- HTML5, CSS3, JavaScript (vanilla)
- LocalStorage для сохранения прогресса
- Адаптивный дизайн (mobile-first)

---

## Структура проекта

```
Prompt2.0/
├── index.html                    # Главная страница курса (все модули)
├── DOCUMENTATION.md              # Эта документация
├── программа-курса.md            # Программа курса
│
├── Модуль-3-Продвинутые-техники-промптинга/
│   └── presentation/
│       ├── index.html            # Главная страница модуля
│       ├── lesson-1.html         # Chain-of-Thought (CoT)
│       ├── lesson-2.html         # Tree of Thoughts (ToT)
│       ├── lesson-3.html         # Self-Consistency
│       ├── lesson-4.html         # ReAct
│       ├── lesson-5.html         # Практика CoT
│       ├── lesson-6.html         # Практика продвинутые техники
│       ├── project.html          # Домашнее задание
│       │
│       ├── css/
│       │   └── styles.css        # Все стили модуля
│       │
│       └── js/
│           ├── app.js            # Основная логика приложения
│           ├── admin.js          # Система администратора
│           └── quiz.js           # Логика квизов
│
├── module-1/                     # Заготовка модуля 1
├── Модуль-2-Архитектура-промптов/
├── Модуль_4_Обучение_на_примерах/
├── Модуль 5 - Мультимодальный ИИ/
├── Module-6-Agent-Systems-Automation/
└── Module-7-Security-Prompts/
```

---

## Главная страница курса

**Файл:** `index.html`

### Функционал

1. **Карточки модулей** — 10 модулей курса в виде карточек
2. **Система блокировки** — только разблокированные модули кликабельны
3. **Прогресс-бар** — отображает общий прогресс по курсу
4. **Toast-уведомления** — информирует о заблокированных модулях

### Конфигурация

```javascript
// В index.html
const STORAGE_KEY = 'promptEngineering_completedModules';
const UNLOCKED_MODULES = [3]; // Разблокированные модули
```

### Статусы карточек

| Класс | Описание |
|-------|----------|
| `.locked` | Заблокированный модуль (серый, с замком) |
| `.active` | Текущий доступный модуль (фиолетовый, пульсация) |
| `.completed` | Завершённый модуль (зелёный) |

### Разблокировка модулей

Для разблокировки модуля измените массив `UNLOCKED_MODULES`:

```javascript
const UNLOCKED_MODULES = [1, 2, 3]; // Разблокировать модули 1, 2, 3
```

Или преобразуйте `<div>` в `<a>` с нужной ссылкой:

```html
<!-- Было (заблокировано) -->
<div class="module-card locked" data-module="2">

<!-- Стало (разблокировано) -->
<a href="Модуль-2-.../presentation/index.html" class="module-card active" data-module="2">
```

---

## Модуль 3: Интерактивная платформа

### Структура уроков

| Файл | Урок | Содержание |
|------|------|------------|
| `index.html` | Главная | Обзор модуля, карточки уроков |
| `lesson-1.html` | Урок 1 | Chain-of-Thought (CoT) |
| `lesson-2.html` | Урок 2 | Tree of Thoughts (ToT) |
| `lesson-3.html` | Урок 3 | Self-Consistency |
| `lesson-4.html` | Урок 4 | ReAct |
| `lesson-5.html` | Урок 5 | Практика CoT |
| `lesson-6.html` | Урок 6 | Практика продвинутые |
| `project.html` | Проект | Домашнее задание |

### Компоненты страницы урока

```html
<!-- Заголовок урока -->
<div class="lesson-header">
  <div class="lesson-meta">
    <span class="lesson-number">Урок 1</span>
    <span class="lesson-duration">⏱ 25 минут</span>
  </div>
  <h1 class="lesson-title">Название урока</h1>
  <div class="lesson-objectives">...</div>
</div>

<!-- Заметка для преподавателя (скрыта в режиме студента) -->
<div class="teacher-note">...</div>

<!-- Карточка с контентом -->
<div class="card">
  <div class="card-header">Заголовок</div>
  <div class="card-body">Содержимое</div>
</div>

<!-- Пример промпта -->
<div class="prompt-example">
  <div class="prompt-example-header">
    <span>📝</span>
    <span>Название</span>
  </div>
  <pre>Текст промпта</pre>
</div>

<!-- Квиз -->
<div class="quiz">...</div>

<!-- Навигация -->
<div class="lesson-nav">...</div>
```

---

## Руководство для преподавателя

### Вход в режим преподавателя

**Способ 1:** Кликните 5 раз по логотипу "PromptMaster" в сайдбаре

**Способ 2:** Нажмите `Ctrl + Shift + A`

**Пароль:** `609285`

### Возможности режима преподавателя

1. **Заметки для преподавателя** — видны блоки с таймингом и советами
2. **Разблокировка уроков** — все уроки доступны сразу
3. **Сброс прогресса** — кнопка для сброса прогресса студентов

### Переключение режимов

В шапке страницы есть переключатель:
- **Режим студента** — стандартный вид
- **Режим преподавателя** — расширенный вид

### Элементы только для преподавателя

```html
<!-- Заметка для преподавателя -->
<div class="teacher-note">
  <div class="teacher-note-header">
    Заметка для преподавателя
  </div>
  <div class="teacher-note-content">
    <p><strong>Время:</strong> 25 минут</p>
    <p><strong>Совет:</strong> Начните с примера...</p>
  </div>
</div>
```

---

## Руководство для студента

### Навигация

1. **Боковая панель** — список всех уроков модуля
2. **Прогресс-бар** — показывает пройденные уроки
3. **Кнопки навигации** — внизу каждой страницы

### Прохождение урока

1. Изучите теоретический материал
2. Просмотрите примеры промптов
3. Выполните интерактивные задания
4. Пройдите квиз
5. Нажмите "Отметить как пройденный"

### Чек-листы

На практических занятиях есть чек-листы:

```
☐ Создал промпт с CoT
☐ Протестировал на 3 задачах
☐ Сравнил результаты
```

Кликайте по пунктам для отметки выполнения.

### Сохранение прогресса

Прогресс автоматически сохраняется в браузере (localStorage). При очистке данных браузера прогресс будет потерян.

---

## Добавление новых модулей

### Шаг 1: Создайте структуру папок

```bash
mkdir -p "Модуль-X-Название/presentation/css"
mkdir -p "Модуль-X-Название/presentation/js"
```

### Шаг 2: Скопируйте файлы из Модуля 3

```bash
cp Модуль-3-.../presentation/css/styles.css Модуль-X-.../presentation/css/
cp Модуль-3-.../presentation/js/*.js Модуль-X-.../presentation/js/
```

### Шаг 3: Измените конфигурацию в app.js

```javascript
// Измените эти константы
const MODULE_KEY = 'completedLessons_mX';  // X = номер модуля
const MODULE_ID = X;
const TOTAL_LESSONS = 7;  // Количество уроков
```

### Шаг 4: Измените цветовую схему (опционально)

В `styles.css` измените переменную `--primary`:

```css
:root {
  --primary: #8B5CF6;      /* Фиолетовый (Модуль 3) */
  /* --primary: #3B82F6;   /* Синий */
  /* --primary: #10B981;   /* Зелёный */
  /* --primary: #F59E0B;   /* Оранжевый */
}
```

### Шаг 5: Создайте страницы уроков

Используйте шаблон из существующих уроков, изменив:
- Заголовок и мета-информацию
- Содержимое карточек
- Вопросы квизов
- Навигационные ссылки

### Шаг 6: Разблокируйте модуль на главной странице

В `index.html`:

```javascript
const UNLOCKED_MODULES = [3, X]; // Добавьте номер нового модуля
```

И преобразуйте карточку в ссылку.

---

## Техническая документация

### CSS классы

#### Карточки
| Класс | Описание |
|-------|----------|
| `.card` | Базовая карточка |
| `.card-header` | Заголовок карточки |
| `.card-body` | Содержимое карточки |

#### Алерты
| Класс | Описание |
|-------|----------|
| `.alert` | Базовый алерт |
| `.alert-info` | Информационный (синий) |
| `.alert-success` | Успех (зелёный) |
| `.alert-warning` | Предупреждение (жёлтый) |
| `.alert-danger` | Ошибка (красный) |

#### Промпты
| Класс | Описание |
|-------|----------|
| `.prompt-example` | Контейнер примера |
| `.prompt-example-header` | Заголовок примера |
| `.code-block` | Блок кода с копированием |

#### Интерактивные элементы
| Класс | Описание |
|-------|----------|
| `.quiz` | Контейнер квиза |
| `.checklist` | Чек-лист |
| `.accordion` | Аккордеон |
| `.tabs` | Табы |

### JavaScript API

#### app.js

```javascript
// Инициализация приложения
initApp();

// Проверка завершённости урока
isLessonCompleted(lessonNumber); // returns boolean

// Отметить урок как пройденный
markLessonComplete(lessonNumber);

// Сбросить прогресс
resetProgress();

// Обновить прогресс-бар
updateProgress();
```

#### admin.js

```javascript
// Проверка статуса администратора
isAdmin(); // returns boolean

// Вход администратора
adminLogin(password); // returns boolean

// Выход администратора
adminLogout();

// Показать модальное окно входа
showLoginModal();
```

#### quiz.js

```javascript
// Создание квиза
const quiz = new Quiz(containerElement);

// Создание мульти-квиза
const multiQuiz = new MultiQuiz(containerElement, questions);
```

### LocalStorage ключи

| Ключ | Описание |
|------|----------|
| `completedLessons_m3` | Пройденные уроки Модуля 3 |
| `promptEngineering_completedModules` | Завершённые модули курса |
| `promptMaster_isAdmin` | Статус администратора |
| `promptMaster_teacherMode` | Режим преподавателя |
| `checklist_*` | Состояние чек-листов |

---

## Кастомизация

### Изменение логотипа

В HTML файлах найдите `.sidebar-logo`:

```html
<div class="sidebar-logo">
  <svg>...</svg>
  PromptMaster  <!-- Измените название -->
</div>
```

### Изменение пароля администратора

В `admin.js`:

```javascript
const ADMIN_PASSWORD = '609285'; // Измените на свой
```

### Изменение цветовой палитры

В `styles.css`:

```css
:root {
  --primary: #8B5CF6;           /* Основной цвет */
  --primary-dark: #7C3AED;      /* Тёмный вариант */
  --accent: #10B981;            /* Акцентный цвет */
  --bg-primary: #0f172a;        /* Фон основной */
  --bg-secondary: #1e293b;      /* Фон вторичный */
}
```

### Добавление нового типа квиза

В `quiz.js` создайте новый класс:

```javascript
class CustomQuiz {
  constructor(container, options) {
    this.container = container;
    this.options = options;
    this.init();
  }

  init() {
    // Логика инициализации
  }

  check() {
    // Логика проверки
  }
}
```

---

## Поддержка и контакты

**Разработано для:** BelHard Academy
**Курс:** Prompt Engineering 2.0
**Версия:** 1.0
**Дата:** Декабрь 2025

---

## Changelog

### v1.0 (Декабрь 2025)
- Создана главная страница курса с 10 модулями
- Разработан интерактивный модуль 3 (Продвинутые техники промптинга)
- Реализована система блокировки модулей/уроков
- Добавлены режимы студента/преподавателя
- Интерактивные демо для анализа промптов
- Квизы с обратной связью
- Чек-листы для практических заданий
- Адаптивный дизайн для мобильных устройств
